import React from "react";
import { EditorHeader, EditorMain, EditorContainer } from "./";

const Editor = () => {
  return (
    <EditorContainer>
      <EditorHeader />
      <EditorMain />
    </EditorContainer>
  );
};

export default Editor;
